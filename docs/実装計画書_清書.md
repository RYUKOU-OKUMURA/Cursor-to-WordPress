# 実装計画書（清書版）

**プロジェクト名**: Cursor → WordPress 自動投稿ツール
**バージョン**: 1.1
**作成日**: 2025-01-08
**更新日**: 2025-01-08

---

## 1. 前提・スコープ

- Phase 1はMarkdown→HTML変換＋WordPress REST API投稿（下書き/公開）
- Phase 2/3は将来拡張（JSON-LD/HTML入力/画像）
- ローカル環境: Python 3.8+、`.env`管理、requests/markdown2/python-dotenv

---

## 2. フェーズ構成

| Phase | 内容 | 状態 |
|-------|------|------|
| Phase 0 | 事前確認・環境準備 | ✅ 完了 |
| Phase 1 | MVP実装（必須機能） | 未着手 |
| Phase 2 | 追加機能（Front Matter, JSON-LD, HTML入力） | 未着手 |
| Phase 3 | 画像対応（アップロード・アイキャッチ） | 未着手 |

---

## 3. Phase 0: 事前確認・準備

### 0-1. 前提条件チェック
- [x] WordPress 5.6以上であることを確認
- [x] Application Passwordsが有効であることを確認
- [x] 投稿権限（投稿者以上）を持つユーザーを用意
- [x] `https://サイトURL/wp-json/` へのアクセス確認

### 0-2. ローカル環境準備
- [x] Pythonバージョン確認（`python --version` で3.8以上） → Python 3.11.7
- [x] 仮想環境作成（`python -m venv venv`）
- [x] 仮想環境有効化（`source venv/bin/activate` or `venv\Scripts\activate`）

### 0-3. 依存関係準備
- [x] `requirements.txt` 作成
- [x] `pip install -r requirements.txt` 実行

### 0-4. 設定ファイル準備
- [x] `.gitignore` 作成（`.env`, `venv/`, `__pycache__/`, `*.pyc`）
- [x] `.env.example` 作成（認証情報のテンプレート）
- [x] `.env` 作成、認証情報入力

### 0-5. 事前疎通テスト
- [x] 最小POSTで認証・投稿確認（draft） → 投稿ID: 1601 で成功
- [ ] 401（認証失敗）時の対処法をメモ
- [ ] 403（権限不足）時の対処法をメモ
- [ ] 404（URL誤り）時の対処法をメモ

### 成果物
- [x] `requirements.txt`
- [x] `.gitignore`
- [x] `.env.example`
- [ ] 動作確認メモ（`docs/疎通テスト結果.md`）

---

## 4. Phase 1: MVP実装

### 1-1. リポジトリ基盤
- [ ] `README.md` 雛形作成
- [ ] `articles/` ディレクトリ作成
- [ ] `articles/sample_article.md` サンプル記事作成

### 1-2. スクリプト骨格（`post_to_wp.py`）
- [ ] `main()` 関数作成
- [ ] argparse によるCLI引数定義
- [ ] `if __name__ == "__main__":` エントリーポイント

### 1-3. 設定読み込み
- [ ] `load_config()` 関数作成
- [ ] dotenv読み込み処理
- [ ] 必須キー検証（`WP_URL`, `WP_USER`, `WP_APP_PASSWORD`）
- [ ] 設定値不足時のエラーメッセージ

### 1-4. ファイル読み込み
- [ ] `read_markdown_file()` 関数作成
- [ ] ファイル存在チェック
- [ ] 読み取り権限チェック
- [ ] UTF-8エンコーディング指定

### 1-5. タイトル抽出
- [ ] `extract_title()` 関数作成
- [ ] H1（`# タイトル`）からの抽出
- [ ] H1がなければH2（`## タイトル`）から抽出
- [ ] 見出しがなければファイル名から生成

### 1-6. Markdown→HTML変換
- [ ] `convert_markdown_to_html()` 関数作成
- [ ] markdown2ライブラリ使用
- [ ] extras設定（`fenced-code-blocks`, `tables`, `code-friendly`）

### 1-7. WordPress投稿
- [ ] `post_to_wordpress()` 関数作成
- [ ] Basic認証（Base64エンコード）
- [ ] `status` パラメータ切替（`draft` / `publish`）
- [ ] タイムアウト設定（30秒）
- [ ] 接続エラーハンドリング

### 1-8. 結果表示/エラー処理
- [ ] `print_success()` 関数作成（投稿ID・編集URL・公開URL表示）
- [ ] `print_error()` 関数作成
- [ ] ステータスコード別メッセージ整備
  - [ ] 201: 成功
  - [ ] 400: リクエスト不正
  - [ ] 401: 認証失敗
  - [ ] 403: 権限不足
  - [ ] 404: エンドポイント不正
  - [ ] 500: サーバーエラー

### 1-9. CLIオプション整備
- [ ] `--draft` オプション（デフォルト）
- [ ] `--publish` オプション
- [ ] `--help` 説明文

### 1-10. ロギング機能
- [ ] logging モジュール設定
- [ ] デバッグレベル切替（`--verbose` オプション）
- [ ] エラー時のスタックトレース出力

### 1-11. ドキュメント整備
- [ ] README.md に使用方法記載
- [ ] `.env` 記入例記載
- [ ] トラブルシューティングセクション作成

### 成果物
- [ ] `post_to_wp.py`
- [ ] `README.md`（完成版）
- [ ] `articles/sample_article.md`

### 受け入れ基準
- [ ] T-01: Markdownファイルを下書き投稿できる
- [ ] T-02: Markdownファイルを公開投稿できる
- [ ] T-03: 存在しないファイル指定時にエラーメッセージ表示
- [ ] T-04: 認証失敗時にエラーメッセージ表示
- [ ] T-05: 投稿成功時に投稿ID・編集URL・公開URLが表示される

---

## 5. Phase 2: 追加機能

### 2-1. Front Matter対応
- [ ] `parse_front_matter()` 関数作成
- [ ] YAML解析（`pyyaml` 追加）
- [ ] タイトル抽出（`title:`）
- [ ] カテゴリ抽出（`categories:`）
- [ ] タグ抽出（`tags:`）
- [ ] スラッグ抽出（`slug:`）
- [ ] 投稿日時抽出（`date:`）
- [ ] 抜粋抽出（`excerpt:`）

### 2-2. カテゴリ/タグのID解決
- [ ] `get_category_id()` 関数作成（名前→ID検索）
- [ ] `get_tag_id()` 関数作成（名前→ID検索）
- [ ] 存在しない場合の自動作成オプション（`--create-terms`）
- [ ] ID解決失敗時の警告メッセージ

### 2-3. JSON-LD移動
- [ ] `move_jsonld_to_end()` 関数作成
- [ ] `<script type="application/ld+json">` 検出
- [ ] HTML末尾への移動処理

### 2-4. HTML入力対応
- [ ] ファイル拡張子判定処理
- [ ] `.html` の場合は変換スキップ
- [ ] `.md` / `.markdown` の場合は変換実行

### 2-5. 投稿パラメータ拡張
- [ ] `categories` パラメータ対応
- [ ] `tags` パラメータ対応
- [ ] `slug` パラメータ対応
- [ ] `date` パラメータ対応
- [ ] `excerpt` パラメータ対応

### 成果物
- [ ] 追加機能の対応関数
- [ ] `requirements.txt` 更新（pyyaml追加）
- [ ] README.md 追記（Front Matter記法の説明）

### 受け入れ基準
- [ ] Front Matterからタイトル/カテゴリ/タグが反映される
- [ ] JSON-LDが本文末尾に配置される
- [ ] HTMLファイルがそのまま投稿できる

---

## 6. Phase 3: 画像対応

### 3-1. ローカル画像検出
- [ ] `find_local_images()` 関数作成
- [ ] `![alt](path)` パターン抽出（正規表現）
- [ ] 相対パス/絶対パスの判定
- [ ] 外部URL（`http://`, `https://`）の除外

### 3-2. メディアアップロード
- [ ] `upload_image()` 関数作成
- [ ] `/wp-json/wp/v2/media` へPOST
- [ ] 対応形式: JPEG, PNG, GIF, WebP
- [ ] Content-Type設定（MIMEタイプ判定）
- [ ] alt属性の設定
- [ ] 重複チェック（同名ファイルの扱い）

### 3-3. パス置換
- [ ] `replace_image_paths()` 関数作成
- [ ] 本文内のローカルパス→アップロード後URLに置換
- [ ] 置換マッピングのログ出力

### 3-4. アイキャッチ設定
- [ ] 最初の画像IDを `featured_media` に設定
- [ ] Front Matterでの明示指定対応（`featured_image:`）
- [ ] アイキャッチなしオプション（`--no-featured`）

### 成果物
- [ ] 画像対応ユーティリティ関数群
- [ ] README.md 追記（画像対応の説明）
- [ ] `articles/sample_with_images.md` サンプル

### 受け入れ基準
- [ ] ローカル画像がWordPressにアップロードされる
- [ ] 本文内の画像パスがURLに置換される
- [ ] アイキャッチ画像が設定される

---

## 7. 依存関係と順序

```
Phase 0 ──→ Phase 1 ──→ Phase 2
                   └──→ Phase 3
```

- Phase 1はPhase 0の環境準備が前提
- Phase 2/3はPhase 1完了後に並行実施可能
- 画像対応はFront Matter対応の有無に影響しないが、投稿API部分が安定していることが前提

---

## 8. テスト戦略

### ユニットテスト（Phase 1完了後に整備推奨）
- [ ] `test_extract_title()`: タイトル抽出のテスト
- [ ] `test_convert_markdown_to_html()`: Markdown変換のテスト
- [ ] `test_load_config()`: 設定読み込みのテスト

### 統合テスト
- [ ] 実際のWordPress環境での投稿テスト
- [ ] 各エラーパターンの再現テスト

---

## 9. セキュリティ考慮事項

- [ ] `.env` は絶対にコミットしない（`.gitignore` 必須）
- [ ] Application Passwordは投稿に必要な最小権限のユーザーで発行
- [ ] HTTPS必須（HTTP環境では警告表示）

---

## 10. 補足: 事前テストを入れる場合

- 最小POSTの疎通テスト（draft投稿）
  - 認証成功 / 権限不足 / URL誤り を初期段階で切り分け可能

```python
# 疎通テスト用スニペット
import requests
from requests.auth import HTTPBasicAuth

response = requests.post(
    "https://your-site.com/wp-json/wp/v2/posts",
    auth=HTTPBasicAuth("username", "application_password"),
    json={"title": "テスト投稿", "content": "テスト", "status": "draft"},
    timeout=30
)
print(response.status_code, response.json())
```
